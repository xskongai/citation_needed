[project]
name = "citation-needed"
version = "0.6.0"
description = "Traceable claim-citation-evidence judgement scaffold with Relation Judge v1.2 + Source Assessor v1.4"
requires-python = ">=3.11"
dependencies = [
  "pydantic>=2.7,<3",
  "openai>=3.5,<4",
]

[tool.pytest.ini_options]
pythonpath = ["."]
