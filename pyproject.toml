[project]
name = "citation-needed"
version = "0.7.0"
description = "Traceable citation audit with Relation Judge v1.2 + Source Assessor v1.4 + deterministic Reliability Policy v1"
requires-python = ">=3.11"
dependencies = [
  "pydantic>=2.7,<3",
  "openai>=3.5,<4",
]

[tool.pytest.ini_options]
pythonpath = ["."]
