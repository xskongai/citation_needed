[project]
name = "citation-needed-v0"
version = "0.3.0"
description = "Traceable claim-citation-evidence judgement scaffold with semantic Judge v1"
requires-python = ">=3.11"
dependencies = [
  "pydantic>=2.7,<3",
  "openai>=3.5,<4",
]

[tool.pytest.ini_options]
pythonpath = ["."]
