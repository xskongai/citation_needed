[project]
name = "citation-needed"
version = "0.8.0"
description = "Traceable citation audit with guarded citation-bearing assertion extraction, relation judgement, source assessment, and reliability policy"
requires-python = ">=3.11"
dependencies = [
  "pydantic>=2.7,<3",
  "openai>=3.5,<4",
]

[tool.pytest.ini_options]
pythonpath = ["."]
